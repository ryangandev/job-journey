import { Inter, Beau_Rivage } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

const beauRivage = Beau_Rivage({
  subsets: ['latin'],
  weight: '400',
});

export { inter, beauRivage };
